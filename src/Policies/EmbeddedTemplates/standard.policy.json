{
  "$schema": "https://raw.githubusercontent.com/agile-north/rexo/schema-v1.0/policy.schema.json",
  "schemaVersion": "1.0",
  "name": "standard-policy",
  "description": "Standard lifecycle policy template with user-facing commands.",
  "commands": {
    "plan": {
      "description": "Validate and show what would be built/pushed.",
      "options": {
        "push": {
          "type": "bool",
          "default": false
        }
      },
      "steps": [
        {
          "id": "validate",
          "uses": "builtin:validate"
        },
        {
          "id": "version",
          "uses": "builtin:resolve-version"
        },
        {
          "id": "plan",
          "uses": "builtin:plan-artifacts",
          "with": {
            "push": "{{options.push}}"
          }
        }
      ]
    },
    "validate": {
      "description": "Validate repository configuration.",
      "options": {},
      "steps": [
        {
          "id": "validate",
          "uses": "builtin:validate"
        }
      ]
    },
    "version": {
      "description": "Resolve repository version.",
      "options": {},
      "steps": [
        {
          "id": "version",
          "uses": "builtin:resolve-version"
        }
      ]
    },
    "test": {
      "description": "Run configured tests.",
      "options": {},
      "steps": [
        {
          "id": "test",
          "uses": "builtin:test"
        }
      ]
    },
    "analyze": {
      "description": "Run configured analysis.",
      "options": {},
      "steps": [
        {
          "id": "analyze",
          "uses": "builtin:analyze"
        }
      ]
    },
    "verify": {
      "description": "Run validation, tests, and analysis.",
      "options": {},
      "steps": [
        {
          "id": "validate",
          "uses": "builtin:validate"
        },
        {
          "id": "test",
          "uses": "builtin:test"
        },
        {
          "id": "analyze",
          "uses": "builtin:analyze"
        }
      ]
    },
    "build": {
      "description": "Build and tag configured artifacts locally.",
      "options": {},
      "steps": [
        {
          "id": "validate",
          "uses": "builtin:validate"
        },
        {
          "id": "version",
          "uses": "builtin:resolve-version"
        },
        {
          "id": "build",
          "uses": "builtin:build-artifacts"
        },
        {
          "id": "tag",
          "uses": "builtin:tag-artifacts"
        }
      ]
    },
    "tag": {
      "description": "Tag configured artifacts.",
      "options": {},
      "steps": [
        {
          "id": "version",
          "uses": "builtin:resolve-version"
        },
        {
          "id": "tag",
          "uses": "builtin:tag-artifacts"
        }
      ]
    },
    "push": {
      "description": "Push configured artifacts when explicitly confirmed.",
      "options": {
        "confirm": {
          "type": "bool",
          "default": false
        }
      },
      "steps": [
        {
          "id": "push",
          "uses": "builtin:push-artifacts",
          "with": {
            "confirm": "{{options.confirm}}"
          }
        }
      ]
    },
    "release": {
      "description": "Validate, verify, build, tag, and optionally push configured artifacts.",
      "options": {
        "push": {
          "type": "bool",
          "default": false
        }
      },
      "steps": [
        {
          "id": "validate",
          "uses": "builtin:validate"
        },
        {
          "id": "version",
          "uses": "builtin:resolve-version"
        },
        {
          "id": "verify",
          "uses": "builtin:verify"
        },
        {
          "id": "build",
          "uses": "builtin:build-artifacts"
        },
        {
          "id": "tag",
          "uses": "builtin:tag-artifacts"
        },
        {
          "id": "push",
          "uses": "builtin:push-artifacts",
          "when": "{{options.push}}",
          "with": {
            "confirm": "{{options.push}}"
          }
        }
      ]
    },
    "clean": {
      "description": "Remove generated Rexo output (artifacts/)",
      "options": {},
      "steps": [
        {
          "id": "clean",
          "uses": "builtin:clean"
        }
      ]
    }
  },
  "aliases": {
    "all": "release",
    "ship": "push"
  }
}
